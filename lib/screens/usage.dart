import 'package:flutter/Material.dart';

class UsageRecord extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("UsageRecord"),
    );
  }
}
